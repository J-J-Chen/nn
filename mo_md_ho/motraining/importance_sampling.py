import torch

class ImportMC():
  def __init__(self):
    self.dist = None

  def metro_sampler(x0, lnprob_fn, prop_fn, prop_fn_kwargs={}, iterations=10):
    return torch.linspace(

  def integrate(eq1, eq2):
    sample_dist = 
    weight_func = torch.mean(eq1*eq2)
    return 0
